version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9
    steps:
       - checkout
       - run: npm install
       - run: CHROME_BIN=google-chrome npm test
       - run: npm run lint

workflows:
  version: 2
  build_and_test:
    jobs:
      - build